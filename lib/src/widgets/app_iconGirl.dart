import 'package:flutter/material.dart';

class AppGirl extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset('images/girl.png', width: 100,)
      ],      
    );
  }
}