import 'package:flutter/material.dart';

class AppBoy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset('images/boy.png', width: 100,)
      ],      
    );
  }
}