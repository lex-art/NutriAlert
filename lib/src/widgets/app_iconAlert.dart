import 'package:flutter/material.dart';

class AppIconAlert extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Image.asset('images/alert.png', width: 30,)
      ],      
    );
  }
}